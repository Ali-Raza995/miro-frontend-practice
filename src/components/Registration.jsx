
// eslint-disable-next-line react/prop-types
const Registration = ({ defaultValue }) => {
  return (
    <div>
      <p>Registration Component with default value: {defaultValue}</p>
    </div>
  );
};

export default Registration;